{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Komandos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Pavadinimas</th>
                <th>Miestas</th>
                <th>Logotipas</th>
                <th>Sukis</th>
                <th>Treneris</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('komandos_show', { 'id': entity.id }) }}">{{ entity.pavadinimas }}</a></td>
                <td>{{ entity.miestas }}</td>
                <td>{{ entity.logotipas }}</td>
                <td>{{ entity.sukis }}</td>
                <td>{{ entity.treneris }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('komandos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('komandos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('komandos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
