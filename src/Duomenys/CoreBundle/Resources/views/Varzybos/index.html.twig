{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Varzybos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Laikas</th>
                <th>Taskaik1</th>
                <th>Taskaik2</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('varzybos_show', { 'id': entity.id }) }}">{{ entity.laikas }}</a></td>
                <td>{{ entity.taskaiK1 }}</td>
                <td>{{ entity.taskaiK2 }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('varzybos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('varzybos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('varzybos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
